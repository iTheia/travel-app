import { Dimensions } from 'react-native';

export const { width, height } = Dimensions.get('window');

export const MAX_HEADER_HEIGHT = 150;

export const cardWidth = width / 3;
export const cardHeight = height / 2.5;
